# Stripe Checkout — never commit real values.
# Test keys (sk_test_ / pk_test_) for local + Vercel Preview.
# Live keys (sk_live_ / pk_live_) only on Vercel Production.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
